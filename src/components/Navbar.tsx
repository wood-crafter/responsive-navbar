import './index.css'

interface IMenuItem {
  menuNo: number;
  // menuName: string;
  // displayYn: string;
  // menuPath: string;
  // sortNo: number;
  // readYn: string;
  // useYn: string;
  // writeYn: string;
  // parentMenuNo?: number | null;
}


function Navbar(menu: IMenuItem[]) {
  return (
    <div className="navbar">
      navbar
    </div>
  );
}

export default Navbar;
